# ── Dependencies ──
node_modules/
server/node_modules/
app/node_modules/

# ── Environment variables ──
.env
.env.local
.env.*.local
server/.env
server/.env.local

# ── Expo ──
app/.expo/
app/dist/
app/web-build/
app/expo-env.d.ts

# ── React Native / Metro ──
app/.metro-health-check*
*.jsbundle

# ── Native build artifacts (in case of prebuild/eject) ──
app/ios/
app/android/

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── OS junk ──
.DS_Store
Thumbs.db

# ── Editor/IDE ──
.vscode/
.idea/
*.swp

# ── Build output ──
server/dist/
server/build/

# ── Misc ──
.eslintcache
*.tsbuildinfo